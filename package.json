{
  "name": "nft-mint-underwriter",
  "version": "0.1.0",
  "description": "Telegram bot that underwrites NFT mints — bytecode gate, exit simulation, and a scheduled mint executor — on Base and Robinhood Chain.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "solc": "^0.8.36"
  }
}
